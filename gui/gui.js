function insertDown(){
    var piece = new Piece(TRACK_TYPES.DOWN);
    TRACK.insertPiece(piece)
}

function insertFlat(){
    var piece = new Piece(TRACK_TYPES.FLAT);
    TRACK.insertPiece(piece)
}

function insertUp(){
    var piece = new Piece(TRACK_TYPES.UP);
    TRACK.insertPiece(piece)
}